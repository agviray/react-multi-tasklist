import styled from 'styled-components';

export const StyledHeader = styled.header`
  position: sticky;
  top: 0;
  left: 0;
  width: 100%;
  padding: 1.75rem;
  background-color: white;
  z-index: 10;

  h1 {
    text-align: center;
    font-size: 30px;
  }
`;
