import React from 'react';
import { StyledListButton } from './styles/ListButton.styled';

const ListButton = () => {
  return <StyledListButton>Save</StyledListButton>;
};

export default ListButton;
